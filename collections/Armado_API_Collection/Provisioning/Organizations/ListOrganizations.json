{
  "name": "List Organizations",
  "request": {
    "method": "GET",
    "header": [
      {
        "key": "X-API-Key",
        "value": "{{api_key}}",
        "type": "text"
      }
    ],
    "url": {
      "raw": "{{base_url}}/provisioning/organizations",
      "host": [
        "{{base_url}}"
      ],
      "path": [
        "provisioning",
        "organizations"
      ]
    },
    "description": "List all organizations"
  }
}